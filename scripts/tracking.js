function search_computer(input) {
	// input=input.toUpperCase();
	switch(input) {
		case "QU603AW":
			document.getElementById('computerfamily').value = 'HP 6200 Pro';
			document.getElementById('formfactor').value = 'Desktop';
			break;
		default:
	}
}